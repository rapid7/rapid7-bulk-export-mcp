# Що нового: інтеграція з InsightIDR / What's New: InsightIDR Integration

*Цей файл відстежує зміни, повʼязані саме з розширенням сервера підтримкою Rapid7 InsightIDR — новий функціонал, а не форк в цілому. / This file tracks changes specific to the server's InsightIDR extension — the new functionality, not the fork as a whole.*

## Про проєкт / About the Project

| Українською | English |
|---|---|
| Цей репозиторій — форк офіційного `rapid7/rapid7-bulk-export-mcp`: гібридний MCP-сервер (Model Context Protocol), який дозволяє AI-асистентам (Claude Desktop, Claude Code, Kiro тощо) працювати з даними Rapid7 напряму через природну мову. | This repository is a fork of the official `rapid7/rapid7-bulk-export-mcp`: a hybrid MCP (Model Context Protocol) server that lets AI assistants (Claude Desktop, Claude Code, Kiro, etc.) work with Rapid7 data directly through natural language. |
| Базовий (upstream) функціонал: експорт даних вразливостей з **Rapid7 InsightVM** через Bulk Export API (GraphQL), завантаження в локальну DuckDB-базу та SQL-аналіз через MCP-інструменти. | Baseline (upstream) functionality: exporting vulnerability data from **Rapid7 InsightVM** via the Bulk Export API (GraphQL), loading it into a local DuckDB database, and SQL analysis through MCP tools. |
| Наше розширення: додаємо підтримку **Rapid7 InsightIDR** (SIEM/XDR) — роботу з investigations (розслідуваннями інцидентів) через REST Command Platform API, той самий сервер, той самий ключ доступу. | Our extension: adding support for **Rapid7 InsightIDR** (SIEM/XDR) — working with investigations (incident investigations) via the REST Command Platform API, same server, same access key. |
| Розгортання: локально, поруч із Claude Desktop / Claude Code, запуск через `uv`, транспорт `stdio`. | Deployment: local, alongside Claude Desktop / Claude Code, launched via `uv`, `stdio` transport. |
| Автентифікація: єдиний ключ `RAPID7_API_KEY` + `RAPID7_REGION` (Organization Key з Command Platform) — без окремих секретів для кожного продукту Rapid7. | Authentication: a single `RAPID7_API_KEY` + `RAPID7_REGION` (an Organization Key from the Command Platform) — no separate secrets per Rapid7 product. |

## Обґрунтування / Rationale

| Українською | English |
|---|---|
| Управління вразливостями (InsightVM) і виявлення/розслідування загроз (InsightIDR) — дві частини одного циклу безпеки, які часто аналізуються разом (наприклад: чи є критична вразливість на активі, де щойно зафіксовано підозрілу активність). | Vulnerability management (InsightVM) and threat detection/investigation (InsightIDR) are two parts of the same security lifecycle, often analyzed together (e.g. whether a critical vulnerability exists on an asset where suspicious activity was just detected). |
| Об'єднання обох продуктів в одному MCP-сервері дозволяє AI-асистенту відповідати на такі наскрізні питання без перемикання між інструментами чи вікнами. | Combining both products in a single MCP server lets the AI assistant answer such cross-cutting questions without switching between tools or windows. |
| Command Platform API вже надає єдиний ендпоінт, автентифікацію і дизайн для обох продуктів — розширення наявного клієнта природніше й дешевше, ніж створення окремого сервера з нуля. | The Command Platform API already provides a single endpoint, authentication, and design shared across both products — extending the existing client is more natural and cheaper than building a separate server from scratch. |
| Форк офіційного open-source репозиторію Rapid7 дає готову інфраструктуру (конфіг, тестування, пакування, DuckDB-шар) — розширюємо перевірений код, а не пишемо MCP-сервер з нуля. | Forking Rapid7's official open-source repository provides ready-made infrastructure (config, testing, packaging, DuckDB layer) — we extend proven code instead of writing an MCP server from scratch. |

## Структура компонентів та залежності / Component Structure & Dependencies

Потік виклику для будь-якого InsightIDR MCP-інструменту, зверху вниз (АІ-асистент → Rapid7 API) і назад: / The call flow for any InsightIDR MCP tool, top to bottom (AI assistant → Rapid7 API) and back:

```
AI-асистент (Claude Desktop / Claude Code)
    │  викликає MCP tool: get_investigations / get_investigation_details /
    │  get_alert_evidence / close_investigation
    ▼
src/mcp_server.py
    │  @mcp.tool функції: приймають аргументи від AI, форматують відповідь
    │  у людяний текст, ловлять помилки. Для close_investigation — тут
    │  живе обов'язковий preview/confirm safety-flow.
    ▼
src/insightidr_manager.py
    │  бізнес-логіка: list_investigations, get_investigation,
    │  list_investigation_alerts, get_alert_evidence, close_investigation.
    │  Валідує статуси (VALID_STATUSES) і dispositions (VALID_DISPOSITIONS)
    │  до того, як щось піде в мережу.
    ▼
src/insightidr_client.py
    │  send_idr_request(): REST-транспорт — X-Api-Key, Content-Type,
    │  User-Agent (з config.py), опційний Accept-version для preview API.
    ▼
Rapid7 InsightIDR REST API  (Command Platform, /idr/v2/*, /idr/at/*)
    ▲
    │  base URL (idr_base) + RAPID7_API_KEY
src/config.py
    load_config(): читає RAPID7_API_KEY (env → macOS Keychain fallback) і
    RAPID7_REGION, будує idr_base = IDR_BASE_ENDPOINTS[region].
```

| Компонент | Українською | English |
|---|---|---|
| `src/config.py` | Єдина точка входу для credentials. `IDR_BASE_ENDPOINTS` — регіональна мапа base URL, окрема від `REGION_ENDPOINTS` (GraphQL Bulk Export). Використовується і InsightVM-, і InsightIDR-частиною. | Single entry point for credentials. `IDR_BASE_ENDPOINTS` — a regional base-URL map, separate from `REGION_ENDPOINTS` (GraphQL Bulk Export). Used by both the InsightVM and InsightIDR parts. |
| `src/insightidr_client.py` | Не знає нічого про investigations/alerts — лише вміє відправити REST-запит з правильними заголовками. Аналог `graphql_client.py`, але для JSON REST замість GraphQL. Не залежить від `insightidr_manager.py`. | Knows nothing about investigations/alerts — it only knows how to send a REST request with the right headers. Analogous to `graphql_client.py`, but for JSON REST instead of GraphQL. Does not depend on `insightidr_manager.py`. |
| `src/insightidr_manager.py` | Тут живуть правила: які статуси/dispositions валідні, який шлях API для кожної дії (наприклад, закриття — через точковий `/status/CLOSED`, а не `bulk_close`). Залежить лише від `insightidr_client.py`. | This is where the rules live: which statuses/dispositions are valid, which API path each action uses (e.g. closing goes through the targeted `/status/CLOSED`, not `bulk_close`). Depends only on `insightidr_client.py`. |
| `src/insightidr_log_search_manager.py` | Окремий manager-файл для Log Search API (LEQL) — інша "родина" API, ніж Investigations/Comments: інший базовий шлях (`/log_search`), без `Accept-version`, асинхронний polling для повільних запитів. Так само залежить лише від `insightidr_client.py`. | A separate manager file for the Log Search API (LEQL) — a different API "family" from Investigations/Comments: a different base path (`/log_search`), no `Accept-version`, async polling for slow queries. Also depends only on `insightidr_client.py`. |
| `src/mcp_server.py` | Єдиний файл, який АІ-асистент бачить напряму — оголошує tools через `@mcp.tool`. Залежить від `insightidr_manager.py` і `insightidr_log_search_manager.py` (бізнес-логіка) і `config.py` (credentials). | The only file the AI assistant sees directly — declares tools via `@mcp.tool`. Depends on `insightidr_manager.py` and `insightidr_log_search_manager.py` (business logic) and `config.py` (credentials). |
| `rapid7-insightidr-skill/SKILL.md` + `README.md` | Не код — документація для АІ-асистента: як і коли викликати tools з `mcp_server.py`, safety-flow, обробка помилок. Оновлюється вручну щоразу, коли зʼявляється новий tool. | Not code — documentation for the AI assistant: how and when to call the tools from `mcp_server.py`, the safety flow, error handling. Updated by hand whenever a new tool appears. |

**Ключова залежність**: `RAPID7_API_KEY` + `RAPID7_REGION` — спільні для InsightVM (GraphQL) і InsightIDR (REST) частин сервера; одна зміна в `config.py` (`load_config()`) впливає на обидві. / **Key dependency**: `RAPID7_API_KEY` + `RAPID7_REGION` are shared between the InsightVM (GraphQL) and InsightIDR (REST) parts of the server; one change in `config.py` (`load_config()`) affects both.

**Окремий, паралельний контур — обслуговування скіла**: `scripts/check_idr_api_spec.py` (запускається через `make check-idr-spec`) не входить у ланцюжок обробки MCP-запитів вище. Він читає/пише `rapid7-insightidr-skill/references/investigations-api-v2.json` і `.spec-meta.json` напряму, незалежно від `RAPID7_API_KEY` (це публічний спек, не потребує автентифікації Rapid7). Це dev-час/maintainer-задача, не рантайм-залежність сервера. / **A separate, parallel loop — skill maintenance**: `scripts/check_idr_api_spec.py` (run via `make check-idr-spec`) is not part of the MCP request-handling chain above. It reads/writes `rapid7-insightidr-skill/references/investigations-api-v2.json` and `.spec-meta.json` directly, independent of `RAPID7_API_KEY` (it's a public spec, no Rapid7 authentication needed). This is a dev-time/maintainer task, not a server runtime dependency.

## Історія змін / Changelog

### 2026-08-27 — Розгортання та тестування базового сервера / Baseline server deployment and testing

| Українською | English |
|---|---|
| Встановлено `uv`, локальне (поза Google Drive) віртуальне середовище для запуску сервера — обхід проблеми `Operation not permitted` при виконанні бінарника з тому Google Drive Desktop. | Installed `uv`, a local (outside Google Drive) virtual environment to run the server — works around an `Operation not permitted` issue when executing a binary from a Google Drive Desktop mount. |
| Підключено MCP-сервер `rapid7-bulk-export` (наявний, немодифікований функціонал InsightVM) до Claude Desktop і до цієї сесії Claude Code. | Connected the `rapid7-bulk-export` MCP server (existing, unmodified InsightVM functionality) to Claude Desktop and to this Claude Code session. |
| Ключ `RAPID7_API_KEY` збережено в macOS Keychain (не в конфігах); `RAPID7_REGION=eu` — у конфігах MCP-клієнтів. | Stored `RAPID7_API_KEY` in macOS Keychain (not in config files); `RAPID7_REGION=eu` — in the MCP client configs. |
| Наскрізно протестовано наявний функціонал: створення export job, очікування завершення, завантаження Parquet у DuckDB, SQL-запити з JOIN — на реальних даних організації. | End-to-end tested the existing functionality: creating an export job, waiting for completion, loading Parquet into DuckDB, SQL queries with JOINs — against the organization's real data. |

### 2026-08-27 — Інтеграція з InsightIDR, Phase 1: Investigations (читання + закриття) / InsightIDR Integration, Phase 1: Investigations (read + close)

| Українською | English |
|---|---|
| Звірено реальну специфікацію InsightIDR Investigations API v2 (preview) з офіційної OpenAPI-схеми: base URL `https://{region}.api.insight.rapid7.com`, шлях `/idr/v2/investigations`, заголовок `Accept-version: investigations-preview`. | Verified the real InsightIDR Investigations API v2 (preview) spec against the official OpenAPI schema: base URL `https://{region}.api.insight.rapid7.com`, path `/idr/v2/investigations`, header `Accept-version: investigations-preview`. |
| `src/config.py`: додано `IDR_BASE_ENDPOINTS` і ключ `idr_base` у `load_config()` — регіональний base URL для Command Platform Capability APIs, окремо від GraphQL-ендпоінту Bulk Export. | `src/config.py`: added `IDR_BASE_ENDPOINTS` and an `idr_base` key in `load_config()` — the regional base URL for Command Platform Capability APIs, separate from the Bulk Export GraphQL endpoint. |
| Новий файл `src/insightidr_client.py`: REST-транспорт (`requests` + `X-Api-Key` + опційний `Accept-version`) — аналог `graphql_client.py`, але для JSON REST замість GraphQL. | New file `src/insightidr_client.py`: REST transport (`requests` + `X-Api-Key` + optional `Accept-version`) — analogous to `graphql_client.py`, but for JSON REST instead of GraphQL. |
| Новий файл `src/insightidr_manager.py`: бізнес-логіка `list_investigations`, `get_investigation`, `list_investigation_alerts`, `close_investigation`; валідація статусів (`OPEN/INVESTIGATING/WAITING/CLOSED`) і dispositions (`BENIGN/MALICIOUS/NOT_APPLICABLE`). | New file `src/insightidr_manager.py`: business logic for `list_investigations`, `get_investigation`, `list_investigation_alerts`, `close_investigation`; validation of statuses (`OPEN/INVESTIGATING/WAITING/CLOSED`) and dispositions (`BENIGN/MALICIOUS/NOT_APPLICABLE`). |
| Закриття investigation навмисно реалізовано через точковий ендпоінт `/investigations/{id}/status/CLOSED` (по одному явному ID), а не через `bulk_close` (закриває все за фільтром) — щоб унеможливити випадкове масове закриття. | Closing an investigation deliberately uses the targeted `/investigations/{id}/status/CLOSED` endpoint (one explicit ID at a time), not `bulk_close` (which closes everything matching a filter) — to make accidental mass-closing impossible. |
| Новий MCP-інструмент `get_investigations` — список/пошук investigations з фільтрами (status, priorities, assignee_email). | New MCP tool `get_investigations` — lists/searches investigations with filters (status, priorities, assignee_email). |
| Новий MCP-інструмент `get_investigation_details` — повний запис одного investigation + пов'язані alerts (докази). | New MCP tool `get_investigation_details` — the full record for one investigation plus its associated alerts (evidence). |
| Новий MCP-інструмент `close_investigation` — write-операція з обов'язковим двоетапним підтвердженням: перший виклик (`confirm=False`, за замовчуванням) лише показує превʼю й нічого не змінює; зміна відбувається тільки при явному `confirm=True`. | New MCP tool `close_investigation` — a write operation with mandatory two-step confirmation: the first call (`confirm=False`, the default) only shows a preview and changes nothing; the change happens only with an explicit `confirm=True`. |
| Тести: `tests/test_insightidr_client.py`, `tests/test_insightidr_manager.py` (табличні, за стилем наявних тестів), оновлено `tests/test_config.py` під новий ключ конфігу `idr_base`. | Tests: `tests/test_insightidr_client.py`, `tests/test_insightidr_manager.py` (table-driven, matching the existing test style), updated `tests/test_config.py` for the new `idr_base` config key. |
| `make lint`, `make security` (bandit), `make test` — усі чисті на момент цього запису. | `make lint`, `make security` (bandit), `make test` — all clean as of this entry. |
| Наскрізно протестовано на реальних даних організації: список 7 investigations, повна деталізація однієї (з alert-доказом), preview закриття без фактичної зміни стану. | End-to-end tested against the organization's real data: listing 7 investigations, full detail on one (with its alert evidence), a close preview with no actual state change. |
| Новий файл `rapid7-insightidr-skill/SKILL.md` — agent skill для аналітики InsightIDR: довідник статусів/dispositions, обов'язковий safety-flow для закриття, типові паттерни тріажу, error handling. | New file `rapid7-insightidr-skill/SKILL.md` — an agent skill for InsightIDR analysis: a reference for statuses/dispositions, the mandatory close safety flow, common triage patterns, error handling. |
| Авторство (`rozumeyroman@gmail.com`) вказано в `SKILL.md` та в докстрінгах нових файлів: `src/insightidr_client.py`, `src/insightidr_manager.py`, `tests/test_insightidr_client.py`, `tests/test_insightidr_manager.py`. | Authorship (`rozumeyroman@gmail.com`) noted in `SKILL.md` and in the docstrings of the new files: `src/insightidr_client.py`, `src/insightidr_manager.py`, `tests/test_insightidr_client.py`, `tests/test_insightidr_manager.py`. |
| Новий файл `rapid7-insightidr-skill/README.md` — за аналогією з README InsightVM-скіла: як встановити/використати скіл, приклади, обмеження. Явно перелічує, які MCP tools вже покриті (3), а які ще ні (Log Search, Detection Rules, Comments, assign/priority без закриття) — щоб документація не забігала наперед реалізації. | New file `rapid7-insightidr-skill/README.md` — modeled on the InsightVM skill's README: how to install/use the skill, examples, limitations. Explicitly lists which MCP tools are already covered (3) and which are not yet (Log Search, Detection Rules, Comments, assign/priority without closing) — so the documentation doesn't get ahead of the implementation. |
| Локальна тестова інфраструктура (не комітиться, у `.gitignore`): `.mcp.json` (MCP-конфіг для Claude Code) та `.claude/skills/` (встановлені скіли для цієї сесії). | Local test infrastructure (not committed, in `.gitignore`): `.mcp.json` (MCP config for Claude Code) and `.claude/skills/` (skills installed for this session). |

### 2026-08-27 — Деталізація подій: новий інструмент `get_alert_evidence` / Event-level detail: new `get_alert_evidence` tool

| Українською | English |
|---|---|
| Після тестування в Claude Desktop виявилось, що `get_investigation_details` повертає лише метадані алертів (тип, час, detection rule) — без IP, користувача чи результату автентифікації. | Testing in Claude Desktop showed that `get_investigation_details` only returns alert metadata (type, timestamp, detection rule) — no IP, user, or authentication result. |
| Досліджено й експериментально перевірено (одним безпечним GET-запитом з реальними даними) окремий ендпоінт alert-triage API: `GET /idr/at/alerts/{alert_rrn}/evidences` — інший шлях (`/idr/at/`), відмінний від Investigations v2 (`/idr/v2/`). | Researched and experimentally verified (one safe GET request against real data) a separate alert-triage API endpoint: `GET /idr/at/alerts/{alert_rrn}/evidences` — a different path (`/idr/at/`) from Investigations v2 (`/idr/v2/`). |
| Спільнота Rapid7 повідомляє, що ця частина API "недорозвинена" і для деяких org потребує окремого увімкнення від підтримки Rapid7 ("restricted evidence") — у нашому org це спрацювало без додаткового звернення. | Rapid7's own community reports this part of the API is "underdeveloped" and for some orgs requires separate enablement from Rapid7 support ("restricted evidence") — in our org it worked without any such request. |
| `src/insightidr_manager.py`: новий `get_alert_evidence(config, alert_id)`. | `src/insightidr_manager.py`: new `get_alert_evidence(config, alert_id)`. |
| Новий MCP-інструмент `get_alert_evidence(alert_id)` — повертає джерело події (user/account, source IP, геолокацію, сервіс, результат) для конкретного alert. | New MCP tool `get_alert_evidence(alert_id)` — returns the source event (user/account, source IP, geolocation, service, result) for a specific alert. |
| **Важливо**: цей виклик повертає реальні персональні дані співробітника (ім'я, email, IP, дані пристрою) — не тестові дані. Додано явне попередження в docstring інструменту та окремий розділ "PRIVACY" у SKILL.md з правилами поводження з такими даними. | **Important**: this call returns a real employee's personal data (name, email, IP, device info) — not test data. Added an explicit warning in the tool's docstring and a dedicated "PRIVACY" section in SKILL.md with rules for handling such data. |
| Тест: `tests/test_insightidr_manager.py::TestGetAlertEvidence`. `make lint`, `make security`, `make test` — усі чисті. | Test: `tests/test_insightidr_manager.py::TestGetAlertEvidence`. `make lint`, `make security`, `make test` — all clean. |
| Оновлено `rapid7-insightidr-skill/SKILL.md` і `README.md`: новий інструмент у списку, приклад використання, розділ про приватність даних. | Updated `rapid7-insightidr-skill/SKILL.md` and `README.md`: new tool listed, usage example, data-privacy section. |
| Додано розділ "Структура компонентів та залежності" у цей файл — потік виклику від АІ-асистента через `mcp_server.py` → `insightidr_manager.py` → `insightidr_client.py` → Rapid7 API, і роль кожного компонента. | Added a "Component Structure & Dependencies" section to this file — the call flow from the AI assistant through `mcp_server.py` → `insightidr_manager.py` → `insightidr_client.py` → the Rapid7 API, and each component's role. |

### 2026-08-27 — Перший реальний тріаж: паттерн "VPN/NOVEL_ASN" у скілі / First real triage: "VPN/NOVEL_ASN" pattern in the skill

| Українською | English |
|---|---|
| Наскрізно протестовано `get_alert_evidence` на реальному investigation ("Suspicious Authentication - Cloudflare Warp"): отримано користувача, IP, сервіс автентифікації, результат, детектор ризику — все з raw event payload. | End-to-end tested `get_alert_evidence` against a real investigation ("Suspicious Authentication - Cloudflare Warp"): retrieved the user, IP, auth service, result, and risk detector — all from the raw event payload. |
| Проаналізовано конкретний кейс: автентифікація через Cloudflare WARP (VPN), детектор `NOVEL_ASN`, один одразу підтверджений Duo push — типовий шаблон "нова мережа для відомого користувача", а не атака. Investigation НЕ закривався — рішення залишено за людиною. | Analyzed the specific case: authentication via Cloudflare WARP (VPN), `NOVEL_ASN` detector, a single immediately-approved Duo push — a typical "new network for a known user" pattern, not an attack. The investigation was NOT closed — the decision was left to the human. |
| За явним запитом користувача: **жодного автоматичного закриття investigations** — це залишається принципом сервера, особливо важливим для інших організацій, які використовуватимуть цей MCP і для яких такий паттерн може бути значно ризикованішим, ніж для поточної. | Per explicit user request: **no automatic closing of investigations** — this remains a server principle, especially important for other organizations that will use this MCP and for whom this pattern may be far riskier than for the current one. |
| Додано розділ "Known Alert Patterns" у `rapid7-insightidr-skill/SKILL.md`: перший задокументований паттерн — VPN/proxy-джерело + `NOVEL_ASN` + одиночний підтверджений MFA push. Включає розпізнавальні ознаки, коротку рекомендацію (щоб не витрачати токени на переказ механізму щоразу), і явний перелік "red flags", які скасовують цей паттерн (кілька push-спроб, відмова користувача, зміна пароля/MFA поруч у часі). | Added a "Known Alert Patterns" section to `rapid7-insightidr-skill/SKILL.md`: the first documented pattern — VPN/proxy source + `NOVEL_ASN` + a single approved MFA push. Includes recognition signature, a terse recommendation (to avoid re-explaining the mechanism every time), and an explicit list of "red flags" that override the pattern (multiple push attempts, user denial, a password/MFA change close in time). |
| Мета паттернів у скілі: закріпити вже "розжовану" експертизу так, щоб надалі схожі інциденти аналізувались за менше токенів — паттерн підказує висновок, але ніколи не замінює обов'язковий preview/confirm перед будь-якою зміною стану. | Purpose of skill patterns: capture already-worked-through expertise so similar future incidents are analyzed with fewer tokens — a pattern suggests a conclusion but never replaces the mandatory preview/confirm before any state change. |

### 2026-08-27 — Призначення investigations: `assign_investigation` / Assigning investigations: `assign_investigation`

| Українською | English |
|---|---|
| Досліджено, чи є API для отримання списку користувачів, яких можна призначити на investigation. Знайдено `GET /account/v1/users` — але він повернув `403 ACCESS_DENIED`: ендпоінт реальний, проте потребує окремих прав (account/user management), яких немає в наявному Organization Key. | Researched whether an API exists to list users eligible for investigation assignment. Found `GET /account/v1/users` — but it returned `403 ACCESS_DENIED`: the endpoint is real but requires separate permissions (account/user management) not held by the current Organization Key. |
| За вказівкою користувача звірено офіційний OpenAPI-спек InsightIDR v2 напряму (`help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json`, знайдений через `spec-url` у HTML сторінки docs) — підтверджено: у самому Investigations API немає вбудованого пошуку/списку eligible-користувачів для assign. | Per the user's direction, verified the official InsightIDR v2 OpenAPI spec directly (`help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json`, found via the `spec-url` in the docs page HTML) — confirmed: the Investigations API itself has no built-in lookup/list of eligible assignee users. |
| Ухвалено комбінований підхід: показувати відомих (раніше призначених) assignee з історії investigations, і водночас дозволяти призначити будь-який email напряму — API сам валідує права (400/403/404). | Adopted a combined approach: show known (previously assigned) assignees from investigation history, while also allowing direct assignment to any email — the API validates permissions itself (400/403/404). |
| `src/insightidr_manager.py`: новий `list_known_assignees(config)` — пагінований збір унікальних assignee з `list_investigations` (з safety cap на кількість сторінок); новий `assign_investigation(config, investigation_id, user_email)` — `PUT /investigations/{id}/assignee`. | `src/insightidr_manager.py`: new `list_known_assignees(config)` — paginated collection of unique assignees via `list_investigations` (with a page-count safety cap); new `assign_investigation(config, investigation_id, user_email)` — `PUT /investigations/{id}/assignee`. |
| Новий MCP-інструмент `list_investigation_assignees()` — read-only, явно позначає себе як неповний список (лише ті, кого вже колись призначали). | New MCP tool `list_investigation_assignees()` — read-only, explicitly marks itself as a non-exhaustive list (only people assigned before). |
| Новий MCP-інструмент `assign_investigation(investigation_id, user_email, confirm?)` — write-операція з тим самим обов'язковим двоетапним підтвердженням (preview → explicit approval → `confirm=True`), що й закриття, оскільки призначена людина отримує email-сповіщення. | New MCP tool `assign_investigation(investigation_id, user_email, confirm?)` — a write operation with the same mandatory two-step confirmation (preview → explicit approval → `confirm=True`) as closing, since the assigned person receives an email notification. |
| Тести: `TestListKnownAssignees` (дедуплікація, пагінація, safety cap, порожній результат), `TestAssignInvestigation` (тіло запиту, заголовок). `make lint`, `make security`, `make test` — усі чисті (114 тестів). | Tests: `TestListKnownAssignees` (deduplication, pagination, safety cap, empty result), `TestAssignInvestigation` (request body, header). `make lint`, `make security`, `make test` — all clean (114 tests). |
| Оновлено `SKILL.md` (новий розділ "Assigning Investigations — SAFETY FLOW", критичні вимоги, error handling) і `README.md` (новий інструмент, приклад використання). | Updated `SKILL.md` (new "Assigning Investigations — SAFETY FLOW" section, critical requirements, error handling) and `README.md` (new tool, usage example). |

### 2026-08-27 — Офіційний OpenAPI-спек як reference-скіл; знайдено й виправлено реальний баг фільтрації / Official OpenAPI spec as a reference skill; found and fixed a real filtering bug

| Українською | English |
|---|---|
| Користувач надав повний офіційний OpenAPI-спек InsightIDR Investigations v2 (JSON з `help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json`) із проханням перетворити на скіл, щоб надалі не шукати ендпоінти заново. | The user provided the full official InsightIDR Investigations v2 OpenAPI spec (JSON from `help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json`), asking to turn it into a skill so endpoints don't need to be re-searched in the future. |
| Перед конвертацією звірили наш код проти спеки — знайдено **реальний баг**: `list_investigations` надсилав query-параметр `status`, тоді як API очікує `statuses` (множина). API мовчки ігнорував невідомий параметр — фільтр не працював взагалі, `get_investigations(status="OPEN")` повертав усі investigations незалежно від статусу. | Before converting, cross-checked our code against the spec — found a **real bug**: `list_investigations` was sending the query param `status`, while the API expects `statuses` (plural). The API silently ignored the unknown param — the filter did nothing at all; `get_investigations(status="OPEN")` returned every investigation regardless of status. |
| Підтверджено наживо (два прямі запити з реальними даними): `status=CLOSED` → 7 з 7 (без фільтрації), `statuses=CLOSED` → 3 з 7 (коректно). Виправлено в `src/insightidr_manager.py`; заодно додано параметри `sources` і `tags` (теж зі спеки), яких раніше не було. | Confirmed live (two direct requests against real data): `status=CLOSED` → 7 of 7 (unfiltered), `statuses=CLOSED` → 3 of 7 (correct). Fixed in `src/insightidr_manager.py`; also added the `sources` and `tags` params (also from the spec), which weren't previously supported. |
| Тест, який раніше **фіксував баг як очікувану поведінку** (`assert "status=OPEN" in request_url`), переписано на правильну перевірку (`assert "statuses=OPEN" in request_url`) з коментарем-поясненням, чому це важливо. | The test that previously **encoded the bug as expected behavior** (`assert "status=OPEN" in request_url`) was rewritten to the correct check (`assert "statuses=OPEN" in request_url`) with an explanatory comment on why it matters. |
| Створено `rapid7-insightidr-skill/references/` — `investigations-api-v2.json` (сира авторитетна специфікація) і `investigations-api-v2.md` (конденсована таблиця: усі 11 ендпоінтів, query-параметри `listInvestigations`, searchable/sortable поля `_search`, enum-значення, пояснення, чому `bulk_close` навмисно не використовується, і нотатка про `updateInvestigation` як майбутню консолідацію кількох single-field викликів). | Created `rapid7-insightidr-skill/references/` — `investigations-api-v2.json` (raw authoritative spec) and `investigations-api-v2.md` (condensed table: all 11 endpoints, `listInvestigations` query params, `_search`'s searchable/sortable fields, enum values, an explanation of why `bulk_close` is deliberately unused, and a note on `updateInvestigation` as a future consolidation of several single-field calls). |
| `SKILL.md`: новий пункт у critical requirements ("check references before guessing"), новий розділ "API Reference", оновлено список prerequisite-tools (`sources`/`tags` у `get_investigations`), виправлено застарілу згадку "API has no free-text title search" (насправді має, через `_search`, просто не підключено як tool). | `SKILL.md`: new critical-requirements bullet ("check references before guessing"), new "API Reference" section, updated the prerequisite-tools list (`sources`/`tags` on `get_investigations`), corrected an outdated note claiming "the API has no free-text title search" (it does, via `_search` — just not wired up as a tool). |
| Урок на майбутнє: перевіряти назви query-параметрів проти спеки, а не покладатись на "розумну" здогадку — невідомий параметр не викликає помилку, він просто мовчки ігнорується, тому баг непомітний без прямого порівняння зі спекою чи живого тесту. | Lesson for the future: verify query param names against the spec rather than relying on a "reasonable" guess — an unknown param doesn't error, it's just silently ignored, so the bug is invisible without a direct spec comparison or a live test. |

### 2026-08-27 — Автоматична перевірка оновлень спеки: `scripts/check_idr_api_spec.py` / Automatic spec-update checking: `scripts/check_idr_api_spec.py`

| Українською | English |
|---|---|
| За пропозицією користувача додано механізм, який раз на тиждень перевіряє, чи змінився офіційний спек за `help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json`, і автоматично завантажує нову версію, якщо є зміни. | Per the user's suggestion, added a mechanism that checks weekly whether the official spec at `help.rapid7.com/insightidr/en-us/api/v2/insightidr-api-v2.json` has changed, and automatically downloads the new version if so. |
| Новий скрипт `scripts/check_idr_api_spec.py`: читає метадані з `rapid7-insightidr-skill/references/.spec-meta.json` (час останньої перевірки, sha256 хеш), пропускає перевірку, якщо минуло < 7 днів (окрім `--force`), інакше завантажує спек, порівнює канонічний хеш (незалежний від порядку ключів/форматування) і — якщо відрізняється — перезаписує `investigations-api-v2.json`, друкує список доданих/видалених API-шляхів і нагадує вручну звірити `investigations-api-v2.md`. | New script `scripts/check_idr_api_spec.py`: reads metadata from `rapid7-insightidr-skill/references/.spec-meta.json` (last-check time, sha256 hash), skips the check if <7 days have passed (unless `--force`), otherwise fetches the spec, compares a canonical hash (independent of key order/formatting), and — if different — overwrites `investigations-api-v2.json`, prints added/removed API paths, and reminds to manually review `investigations-api-v2.md`. |
| Новий Makefile target: `make check-idr-spec` (і `make check-idr-spec FORCE=1` для негайної перевірки) — команда для мейнтейнера, не викликається автоматично і не є MCP tool (АІ-асистент не тягне зовнішній URL під час звичайного тріажу). | New Makefile target: `make check-idr-spec` (and `make check-idr-spec FORCE=1` to check immediately) — a maintainer command, not run automatically and not an MCP tool (the AI assistant doesn't fetch an external URL during normal triage). |
| Функціонально протестовано наживо: `--force` проти реального URL коректно виявив "без змін" (той самий хеш, що і при першому збереженні спеку); без `--force` одразу після цього коректно пропустив перевірку через 7-денний gate. | Functionally tested live: `--force` against the real URL correctly detected "no changes" (same hash as when the spec was first saved); without `--force` immediately after, it correctly skipped the check due to the 7-day gate. |
| Тести: `tests/test_check_idr_api_spec.py` — gate-логіка (межові випадки: рівно 7 днів, зіпсована мітка часу), канонічний хеш (порядок ключів не впливає), diff шляхів, round-trip метаданих, і наскрізний `check_and_update` (skip / no-change / change-downloads / перший запуск без метаданих / мережева помилка не чіпає файли). `make lint`, `make security`, `make test` — усі чисті (131 тест). | Tests: `tests/test_check_idr_api_spec.py` — gate logic (edge cases: exactly 7 days, corrupt timestamp), canonical hash (key order doesn't matter), path diffing, meta round-trip, and end-to-end `check_and_update` (skip / no-change / change-downloads / first run with no meta / network error doesn't touch files). `make lint`, `make security`, `make test` — all clean (131 tests). |
| Оновлено `SKILL.md` і `README.md`: пояснення механізму, явно зазначено — це dev/maintainer workflow, а не інструмент, який АІ використовує під час тріажу інцидентів. | Updated `SKILL.md` and `README.md`: explained the mechanism, explicitly noting it's a dev/maintainer workflow, not a tool the AI uses during incident triage. |

### 2026-08-27 — `make check-idr-spec` підтверджено як dev-only, не прив'язано до MCP-сервера / `make check-idr-spec` confirmed as dev-only, not tied to the MCP server

| Українською | English |
|---|---|
| Користувач запитав, чи перевірка спека відбувається сама під час роботи MCP-сервера. Пояснено і підтверджено разом з користувачем: ні — сервер узагалі не читає reference-файли під час роботи (поведінку визначає захардкоджений код у `insightidr_manager.py`/`insightidr_client.py`), тож прив'язка мережевого виклику до старту/рантайму сервера додала б затримку/ризик збою без жодної функціональної користі. | The user asked whether the spec check runs automatically while the MCP server is running. Explained and confirmed with the user: no — the server never reads the reference files at runtime (behavior comes from hardcoded code in `insightidr_manager.py`/`insightidr_client.py`), so tying a network call to server startup/runtime would add latency/failure risk with no functional benefit. |
| Рішення: `make check-idr-spec` залишається виключно dev-командою для мейнтейнера. | Decision: `make check-idr-spec` remains a dev-only maintainer command. |
| Додано `scripts/README.md`: пояснює скрипт і явно радить мейнтейнеру запускати `make check-idr-spec FORCE=1` **перед** будь-якими змінами в InsightIDR-коді сервера — щоб не покладатись на застарілу локальну копію спека. Коротке нагадування-посилання додано і в `rapid7-insightidr-skill/README.md`. | Added `scripts/README.md`: explains the script and explicitly advises the maintainer to run `make check-idr-spec FORCE=1` **before** any changes to the server's InsightIDR code — so as not to rely on a stale local spec copy. A short reminder/link was also added to `rapid7-insightidr-skill/README.md`. |
| Збережено як пам'ять сесії (feedback-тип): перевіряти актуальність спека перед зміною `insightidr_manager.py`/`insightidr_client.py` — щоб це правило застосовувалось автоматично і в майбутніх розмовах, не лише в цій. | Saved as a session memory (feedback type): check the spec's currency before changing `insightidr_manager.py`/`insightidr_client.py` — so this rule applies automatically in future conversations too, not just this one. |

### 2026-08-27 — Коментарі до investigations: читання + додавання / Investigation comments: read + add

| Українською | English |
|---|---|
| Знайдено й звірено офіційний OpenAPI-спек Comments API (v1, стабільний — на відміну від Investigations v2, **не** потребує заголовка `Accept-version`) тим самим методом, що й для v2: `spec-url` у HTML-сторінці `help.rapid7.com/insightidr/en-us/api/v1/docs.html` → `insightidr-api-v1.json`. | Found and verified the official Comments API OpenAPI spec (v1, stable — unlike Investigations v2, it does **not** need an `Accept-version` header) via the same method as v2: the `spec-url` in the HTML of `help.rapid7.com/insightidr/en-us/api/v1/docs.html` → `insightidr-api-v1.json`. |
| Попутно перевірено `/idr/v1/users/_search` (з того самого v1-спека) на предмет розв'язання раніше відкритого питання "список eligible assignees" — підтверджено: це **UBA-ідентичності** (`rrn:uba:...`, відстежувані мережеві акаунти на кшталт "Євгенія Заруба"), не платформні логін-акаунти. Це не вирішує задачу assign — висновок з попередньої ітерації лишається чинним. | Along the way, checked `/idr/v1/users/_search` (from the same v1 spec) as a possible answer to the earlier open "eligible assignees list" question — confirmed: these are **UBA identities** (`rrn:uba:...`, monitored network accounts like "Євгенія Заруба"), not platform login accounts. This does not solve the assign problem — the previous iteration's conclusion stands. |
| Погоджено обсяг з користувачем: лише читання + додавання коментарів (без delete/visibility-update у цій ітерації); створення коментаря — **без confirm** (на відміну від close/assign), оскільки коментар не змінює стан investigation і не є деструктивним. | Scope agreed with the user: read + add comments only (no delete/visibility-update this iteration); comment creation — **no confirm required** (unlike close/assign), since a comment doesn't change investigation state and isn't destructive. |
| `src/insightidr_manager.py`: новий `list_comments(config, target_rrn, ...)` (`GET /idr/v1/comments?target=...`) і `create_comment(config, target_rrn, body)` (`POST /idr/v1/comments`). Обидва без `Accept-version` заголовка. | `src/insightidr_manager.py`: new `list_comments(config, target_rrn, ...)` (`GET /idr/v1/comments?target=...`) and `create_comment(config, target_rrn, body)` (`POST /idr/v1/comments`). Neither uses an `Accept-version` header. |
| Нові MCP-інструменти: `list_investigation_comments(investigation_id, limit?)` (read-only) і `add_investigation_comment(investigation_id, body)` (write, застосовується одразу — явно задокументовано в docstring, чому це виняток із правила confirm, і що це не прецедент для інших write-операцій). | New MCP tools: `list_investigation_comments(investigation_id, limit?)` (read-only) and `add_investigation_comment(investigation_id, body)` (write, applied immediately — the docstring explicitly explains why this is an exception to the confirm rule, and that it's not a precedent for other write operations). |
| Створено `rapid7-insightidr-skill/references/comments-api-v1.md` (конденсована таблиця) і збережено сирий `insightidr-api-v1.json` (240 КБ, покриває Comments + Accounts/Assets/Attachments/Custom Threats/Collectors/Cloud Webhooks/Users — конденсовано наразі лише Comments, решта позначена як "не каталогізовано" для майбутнього). | Created `rapid7-insightidr-skill/references/comments-api-v1.md` (condensed table) and saved the raw `insightidr-api-v1.json` (240KB, covers Comments + Accounts/Assets/Attachments/Custom Threats/Collectors/Cloud Webhooks/Users — only Comments condensed so far, the rest marked "not yet cataloged" for the future). |
| Важливо: `make check-idr-spec` наразі перевіряє **лише** v2-спек Investigations. v1-спек (Comments) отримано вручну один раз і НЕ покривається автоматичною перевіркою — позначено як явне обмеження в `SKILL.md`. | Important: `make check-idr-spec` currently checks **only** the v2 Investigations spec. The v1 spec (Comments) was fetched by hand once and is NOT covered by the automatic check — flagged as an explicit limitation in `SKILL.md`. |
| Тести: `TestListComments`, `TestCreateComment` у `test_insightidr_manager.py`. `make lint`, `make security`, `make test` — усі чисті (134 тести). | Tests: `TestListComments`, `TestCreateComment` in `test_insightidr_manager.py`. `make lint`, `make security`, `make test` — all clean (134 tests). |
| Оновлено `SKILL.md` (новий розділ "Comments" з обґрунтуванням винятку з правила confirm, новий pattern, error handling), `README.md` (інструменти, приклад), version bump 0.2.0 → 0.3.0. | Updated `SKILL.md` (new "Comments" section explaining the confirm-rule exception, a new pattern, error handling), `README.md` (tools, example), version bump 0.2.0 → 0.3.0. |

### 2026-08-27 — Алерти з інших Rapid7-продуктів: `get_investigation_product_alerts` / Alerts from other Rapid7 products: `get_investigation_product_alerts`

| Українською | English |
|---|---|
| Реалізовано `getInvestigationRapid7ProductAlertInfo` (`GET /idr/v2/investigations/{id}/rapid7-product-alerts`) — раніше задокументований у reference як "not implemented". Повертає алерти з **інших** Rapid7-продуктів (Threat Command, Insight Agent), пов'язаних з investigation — окремо від власних InsightIDR-алертів, які дає `get_investigation_details`. | Implemented `getInvestigationRapid7ProductAlertInfo` (`GET /idr/v2/investigations/{id}/rapid7-product-alerts`) — previously documented in the reference as "not implemented". Returns alerts from **other** Rapid7 products (Threat Command, Insight Agent) linked to the investigation — separate from InsightIDR's own alerts, which `get_investigation_details` covers. |
| Особливість відповіді: чистий JSON-масив, а не типовий `{data, metadata}` — зафіксовано в `references/investigations-api-v2.md`. | Response quirk: a plain JSON array, not the usual `{data, metadata}` shape — noted in `references/investigations-api-v2.md`. |
| `src/insightidr_manager.py`: новий `list_investigation_product_alerts(config, investigation_id)`. Новий MCP-інструмент `get_investigation_product_alerts(investigation_id)` — read-only, форматує тип продукту, для Threat Command показує `alert_id`/`alert_type`/`applicable_close_reasons`, для Insight Agent — `alert_id`/`alert_type`/`agent_action_taken`. | `src/insightidr_manager.py`: new `list_investigation_product_alerts(config, investigation_id)`. New MCP tool `get_investigation_product_alerts(investigation_id)` — read-only, formats product type, showing `alert_id`/`alert_type`/`applicable_close_reasons` for Threat Command, `alert_id`/`alert_type`/`agent_action_taken` for Insight Agent. |
| Виявлено й задокументовано пов'язаний пробіл: `applicable_close_reasons` для Threat Command тепер видно, але `close_investigation` не приймає `threat_command_close_reason` — API-схема (`InvestigationStatusRequestV2`) підтримує це поле, наш код його не передає. Зафіксовано як "known gap" у `references/investigations-api-v2.md` і в `SKILL.md` (розділ "Closing Investigations"), щоб я явно попереджав користувача, а не мовчки ігнорував close reason. | Found and documented a related gap: `applicable_close_reasons` for Threat Command is now visible, but `close_investigation` doesn't accept a `threat_command_close_reason` — the API schema (`InvestigationStatusRequestV2`) supports it, our code doesn't pass it through. Flagged as a "known gap" in `references/investigations-api-v2.md` and in `SKILL.md` (Closing Investigations section), so I explicitly warn the user instead of silently dropping a close reason. |
| Тести: `TestListInvestigationProductAlerts` (успішний список, порожній результат). `make lint`, `make security`, `make test` — усі чисті (136 тестів). | Tests: `TestListInvestigationProductAlerts` (successful list, empty result). `make lint`, `make security`, `make test` — all clean (136 tests). |
| Оновлено `SKILL.md` (новий запис у Data Source з розрізненням "alert" vs "product alert", новий usage pattern, error handling, попередження про Threat Command close reason gap), `README.md`, version bump 0.3.0 → 0.4.0. | Updated `SKILL.md` (new Data Source entry distinguishing "alert" vs "product alert", new usage pattern, error handling, Threat Command close-reason gap warning), `README.md`, version bump 0.3.0 → 0.4.0. |

### 2026-08-27 — Log Search API: query, saved queries, logs/logsets / Log Search API: query, saved queries, logs/logsets

| Українською | English |
|---|---|
| За запитом користувача реалізовано 5 можливостей Log Search API: Query Log Data, Saved Queries, Manage Logs, Manage Logsets (усі — читання/пошук), і CRUD для Saved Queries (create+delete, з preview/confirm). Pre-Computed Queries свідомо виключено — не знайдено підтвердженого API-ресурсу під цю назву. | Per the user's request, implemented 5 Log Search API capabilities: Query Log Data, Saved Queries, Manage Logs, Manage Logsets (all read/search), and Saved Queries CRUD (create+delete, with preview/confirm). Pre-Computed Queries was deliberately excluded — no confirmed API resource was found under that name. |
| Офіційного OpenAPI JSON-спека для Log Search не знайдено (`docs.rapid7.com/insightidr/log-search-api/` рендериться через client-side JS, спек не вдалось знайти навіть у RSC-payload). Замість цього звірено три незалежні джерела: офіційний блог-пост Rapid7 (2020), **офіційна Postman-колекція** Rapid7 (`github.com/rapid7/logentries-postman-collection`) і community-спек (Shuffle, лише для назв шляхів, перевірено наживо). | No official OpenAPI JSON spec was found for Log Search (`docs.rapid7.com/insightidr/log-search-api/` renders via client-side JS; the spec couldn't be located even in the RSC payload). Instead, cross-checked three independent sources: an official Rapid7 blog post (2020), Rapid7's **official Postman collection** (`github.com/rapid7/logentries-postman-collection`), and a community spec (Shuffle, path names only, verified live). |
| Виявлено розбіжність між джерелами: community-спек декларував `servers: {region}.api.insight.rapid7.com/log_search`, але його ж curl-приклади використовували інший хост `{region}.rest.logs.insight.rapid7.com`. Перевірено наживо — **обидва хости працюють ідентично** (той самий бекенд); обрано перший для консистентності з рештою кодової бази. | Found a discrepancy between sources: the community spec declared `servers: {region}.api.insight.rapid7.com/log_search`, but its own curl examples used a different host, `{region}.rest.logs.insight.rapid7.com`. Verified live — **both hosts work identically** (same backend); chose the former for consistency with the rest of the codebase. |
| Ключове відкриття з офіційної Postman-колекції (не було в жодному іншому джерелі): тіло `POST /query/saved_queries` має бути обгорнуте в `{"saved_query": {...}}` — без цього API повертає малоінформативний `400 "A Json Error occurred mapping the request received"`. Кілька спроб "розумної" здогадки (без обгортки) провалились, поки не знайшли офіційний приклад. | Key discovery from the official Postman collection (absent from every other source): the `POST /query/saved_queries` body must be wrapped in `{"saved_query": {...}}` — without it, the API returns an uninformative `400 "A Json Error occurred mapping the request received"`. Several "reasonable guess" attempts (unwrapped) failed before the official example was found. |
| Наскрізно протестовано наживо повний CRUD-цикл для saved query (`create → 201`, `delete → 204`), повний async query-цикл (`POST /query/logs → 202` + polling link → `GET` → `200` з подіями), і всі read-ендпоінти (`management/logs`, `management/logsets`, по одному записі кожного, `query/saved_queries`) — на реальних логах org (O365 ingress-auth). | End-to-end tested live: the full saved query CRUD cycle (`create → 201`, `delete → 204`), the full async query cycle (`POST /query/logs → 202` + polling link → `GET` → `200` with events), and all read endpoints (`management/logs`, `management/logsets`, single-item lookups for each, `query/saved_queries`) — against the org's real logs (O365 ingress-auth). |
| Новий файл `src/insightidr_log_search_manager.py` (окремий від `insightidr_manager.py` — інша API-родина): `list_logs`, `list_logsets`, `query_logs` (з внутрішнім `_poll_until_ready`, що йде за `links[].rel=="self"` до `max_wait_seconds`), `list_saved_queries`, `create_saved_query`, `delete_saved_query`, `run_saved_query`. | New file `src/insightidr_log_search_manager.py` (separate from `insightidr_manager.py` — a different API family): `list_logs`, `list_logsets`, `query_logs` (with an internal `_poll_until_ready` that follows `links[].rel=="self"` up to `max_wait_seconds`), `list_saved_queries`, `create_saved_query`, `delete_saved_query`, `run_saved_query`. |
| 7 нових MCP-інструментів: `list_logs`, `list_logsets`, `query_logs` (privacy-застереження як у `get_alert_evidence`), `list_saved_queries`, `run_saved_query` (те саме застереження), `create_saved_query` і `delete_saved_query` (обидва — preview/confirm, як close/assign). | 7 new MCP tools: `list_logs`, `list_logsets`, `query_logs` (privacy warning like `get_alert_evidence`), `list_saved_queries`, `run_saved_query` (same warning), `create_saved_query` and `delete_saved_query` (both preview/confirm, like close/assign). |
| Створено `rapid7-insightidr-skill/references/log-search-api.md` — з явним застереженням, що це не "verbatim spec" (як `investigations-api-v2.json`), а зведення трьох джерел, і з розділом "Not implemented this iteration" (single log/logset detail, log/logset CRUD — свідомо поза межами через ширший blast radius, PATCH/PUT saved query). | Created `rapid7-insightidr-skill/references/log-search-api.md` — with an explicit note that it's not a "verbatim spec" (unlike `investigations-api-v2.json`), but a synthesis of three sources, plus a "Not implemented this iteration" section (single log/logset detail, log/logset CRUD — deliberately out of scope due to wider blast radius, saved query PATCH/PUT). |
| Тести: `tests/test_insightidr_log_search_manager.py` — 11 тестів, включно з поллінгом (успішний, timeout-кейс) і точним форматом тіла `create_saved_query`. `make lint`, `make security`, `make test` — усі чисті (147 тестів). | Tests: `tests/test_insightidr_log_search_manager.py` — 11 tests, including polling (success, timeout case) and the exact `create_saved_query` body shape. `make lint`, `make security`, `make test` — all clean (147 tests). |
| Оновлено `SKILL.md` (новий розділ "Log Search" з LEQL-основами, privacy-нотатка, safety-flow для saved queries, нові usage patterns, error handling), `README.md` (7 нових інструментів, приклад), `WHATS_NEW_INSIGHTIDR.md` (новий компонент у структурній таблиці), version bump 0.4.0 → 0.5.0. | Updated `SKILL.md` (new "Log Search" section with LEQL basics, privacy note, saved-query safety flow, new usage patterns, error handling), `README.md` (7 new tools, example), `WHATS_NEW_INSIGHTIDR.md` (new component in the structure table), version bump 0.4.0 → 0.5.0. |

### 2026-08-27 — Виправлено критичний баг: `query_logs`/`run_saved_query` завжди повертали 0 подій / Fixed critical bug: `query_logs`/`run_saved_query` always returned 0 events

| Українською | English |
|---|---|
| Користувач протестував у Claude Desktop і повідомив: `query_logs` стабільно повертав 0 подій для будь-якого джерела логу, будь-якого часового вікна (15 хв — 90 днів) і будь-якого LEQL-стейтменту, включно з вікнами, де контрольна перевірка через `get_alert_evidence` підтвердила реальні події. Розслідувано наживо (реальний API, той самий обліковий запис) і знайдено ДВІ окремі реальні причини. | The user tested in Claude Desktop and reported: `query_logs` consistently returned 0 events for every log source, every time window (15 min — 90 days), and every LEQL statement — including windows where a cross-check via `get_alert_evidence` confirmed real events existed. Investigated live (real API, same account) and found TWO separate real root causes. |
| **Причина 1 (баг у нашому коді, критична)**: `_poll_until_ready` вважав запит завершеним, як тільки в відповіді з'являвся ключ `"events"`. Але pending-відповідь (`202`) від API **вже містить** `"events": []` (порожній список, а не відсутній ключ) — різницю між "ще обробляється" і "завершено" видно лише за наявністю/відсутністю `"links"` (self-href). Через це наш код завжди повертав перший-же порожній pending-результат, жодного разу не поллячи. Підтверджено наживо: ручний поллінг того самого query_id дав `50` реальних подій замість `0`. | **Cause 1 (real bug in our code, critical)**: `_poll_until_ready` treated a query as done as soon as `"events"` appeared in the response. But a pending (`202`) API response **already contains** `"events": []` (an empty list, not an absent key) — the real distinction between "still processing" and "final" is only the presence/absence of `"links"` (self-href). Because of this, our code always returned the very first, empty, still-pending result without ever polling. Confirmed live: manually polling the same query_id yielded `50` real events instead of `0`. |
| Виправлено `_poll_until_ready` (`src/insightidr_log_search_manager.py`): умова циклу тепер `while elapsed < max_wait_seconds` з виходом лише коли `links`-self зникає, а не за наявністю `events`. Оновлено логіку "still processing" у `src/mcp_server.py` (`query_logs`, `run_saved_query`) на новий хелпер `_log_query_still_pending()`, що перевіряє `links`, а не `events`. | Fixed `_poll_until_ready` (`src/insightidr_log_search_manager.py`): the loop condition is now `while elapsed < max_wait_seconds`, exiting only when the `links` self-href disappears, not based on `events` presence. Updated the "still processing" logic in `src/mcp_server.py` (`query_logs`, `run_saved_query`) to a new `_log_query_still_pending()` helper that checks `links`, not `events`. |
| **Причина 2 (документаційна пастка)**: LEQL-стейтмент `"where()"` (порожні дужки) — НЕ валідний синтаксис; API повертає `400 {"errorCode":101011,"message":"Invalid Query Syntax"}`. Правильний спосіб "усі події без фільтра" — порожній рядок `statement=""`. Це також пояснює повідомлений користувачем `400` при 11 log_ids — спочатку хибно запідозрено ліміт на кількість log_ids, але наживо підтверджено: 3 log_ids з коректним стейтментом працюють без проблем; причина — саме синтаксис `where()`, а не кількість. | **Cause 2 (a documentation trap)**: the LEQL statement `"where()"` (empty parentheses) is NOT valid syntax — the API returns `400 {"errorCode":101011,"message":"Invalid Query Syntax"}`. The correct way to say "all events, no filter" is an empty string, `statement=""`. This also explains the `400` the user hit with 11 log_ids — initially mis-suspected as a log_id count limit, but confirmed live: 3 log_ids with a valid statement work fine; the real cause was the `where()` syntax, not the count. |
| Додано явне застереження в `SKILL.md` і `references/log-search-api.md`: використовувати `statement=""` для "усі події", не `where()`. Задокументовано весь баг (обидві причини, наживо-підтвердження) у `references/log-search-api.md` для майбутніх maintainer-ів. | Added an explicit warning in `SKILL.md` and `references/log-search-api.md`: use `statement=""` for "all events", not `where()`. Documented the whole bug (both causes, live confirmation) in `references/log-search-api.md` for future maintainers. |
| Оновлено тести (`tests/test_insightidr_log_search_manager.py`): pending fixtures тепер коректно включають `"events": []`, щоб тести відбивали реальну форму відповіді API, а не хибну попередню умову. `make lint`, `make security`, `make test` — усі чисті (147 тестів, без нових). | Updated tests (`tests/test_insightidr_log_search_manager.py`): pending fixtures now correctly include `"events": []`, so the tests reflect the real API response shape instead of the previous incorrect assumption. `make lint`, `make security`, `make test` — all clean (147 tests, no new ones added). |
| **Урок**: та сама категорія помилки, що й з `statuses` у `list_investigations` раніше в цьому проєкті — код "працював" (не падав з помилкою), але мовчки повертав неправильний результат. Знову підтверджує правило: перевіряти наживо з відомим очікуваним результатом, а не лише перевіряти відсутність винятків. | **Lesson**: the same category of bug as the `statuses` issue in `list_investigations` earlier in this project — the code "worked" (didn't throw), but silently returned the wrong result. Reaffirms the rule: verify live against a known expected result, not just the absence of exceptions. |
